-- NOTE: Unreal support for Neovim, potentially quite unstable. Only confirmed on windows!
return {
  {
    "xubury/Unreal.nvim", -- Unreal support for Neovim, fork based on zadirons work
    dependencies = { "tpope/vim-dispatch" },
  },
}
